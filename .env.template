# ZTP Bootstrap environment secrets
# Copy to .env and fill in values before running setup.sh
# This file is NOT sensitive — .env is sensitive and should never be committed

# PostgreSQL password for Kea DHCP backend
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=
